describe('answer handlers', () => {
    describe('handleEnumAnswer', () => {
        it('should work', () => {
            fail();
        });
    });

    describe('handleYesOrNoAnswer', () => {
        it('should work', () => {
            fail();
        });
    });
});
